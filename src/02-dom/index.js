import {idfunction, show, add} from './functions'
main.addEventListener("click", console.log(event.target.id));
Show.addEventListener("click", show);
Add.addEventListener("click", add);
var itemNumber = 4;